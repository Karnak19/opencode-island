import SwiftUI

// MainView is no longer used - keeping for reference
// The Island UI is now in AppDelegate.swift (IslandView)

struct MainView: View {
    var body: some View {
        EmptyView()
    }
}
